User-agent: *
Allow: /

# partials/ and data/ are fetched by the page at render time. Blocking them
# would leave crawlers with an empty header, footer and services grid, so
# they stay crawlable even though they are not pages themselves.

Sitemap: https://legal.oneisok.co/sitemap.xml
